# Optional: Direct API mode (future enhancement)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Claudex uses CLI tools (claude -p, codex exec) by default,
# which authenticate through your Pro/Plus subscriptions.
# No API keys needed for default operation.
